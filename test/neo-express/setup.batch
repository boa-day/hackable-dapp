transfer 15 GAS genesis deployer
transfer 15 GAS genesis user
contract invoke deploy.neo-invoke.json deployer
contract deploy ../test_contract.nef user
checkpoint create --force deployed.neoxp-checkpoint
